{
  "name": "netlify-ably-chat",
  "version": "1.0.0",
  "description": "Real-time chat using Netlify + Ably",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static build complete'",
    "dev": "cd app && python -m http.server 3000",
    "start": "npm run dev"
  },
  "dependencies": {
    "ably": "^1.2.47"
  },
  "devDependencies": {
    "@netlify/functions": "^1.6.0"
  }
}
